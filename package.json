{
  "name": "pills-1",
  "version": "0.0.1",
  "lockfileVersion": 2,
  "requires": true,
  "author": "Gius",
  "description": "",
  "license": "ISC",
  "main": "index.js",
  "packages": {},
  "scripts": {
    "build": "tsc",
    "debug": "ts-node-dev --inspect=0.0.0.0 --respawn -- src/app.ts",
    "run-prod": "tsc && node bin/app.js",
    "start": "ts-node-dev --respawn src/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ts-node-dev": "^2.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.15"
  }
}
